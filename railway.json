{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "dockerfilePath": "Dockerfile",
    "builder": "DOCKERFILE",
    "context": "."
  },
  "deploy": {
    "startCommand": "./backend"
  }
}
