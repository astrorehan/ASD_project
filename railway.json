{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "web/backend/Dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "./web/backend/backend"
  }
}
